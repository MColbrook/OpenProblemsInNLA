# PR #306 MI-27 audit and promotion

Verdict: **APPROVE for merge**, after the subsequent publication/combined CI passes. The audited original PR head is `1f05b398013d44beb7d756cbfbbfd3e875c8deab`. No mathematical, source-trust, frozen-contract, identity or dependency-pin blocker was found. The source-matched external Linux evidence closes the only pending proof-verification gate, so promotion from Solved to Lean verified and one completed original target is justified. Holden's separate order-two sharpness result is explicitly outside the formal scope.

## Independent scope and findings

I inspected the canonical original target, all twenty concrete Challenge contracts, actual definitions and exported signatures, final proof assembly, and the delicate source architecture: endpoint projection optimization for the hockey-stick flow estimate, ordinary noncommuting entropy representation, prior product integrability before Fubini, the trace correction for unnormalized states, complex congruence/inertia, trace-tangent differentiation without eigenvector differentiation, actual Gram-square-root/sign-witness identification, and original-input normalization. This is a fresh source and execution-evidence audit, not a claim to have independently reproduced every line of the 52-module mathematical development by hand.

The final C20 theorem has only the original assumptions: every `n >= 1`, complex positive definite A and B, and the actual complex trace of A+B equal to one. Its right-hand coefficient is exactly one. `logM` is CFC's spectral natural logarithm; `traceNorm` is the real trace of `(Xᴴ*X)^(1/2)`, and the dual `opNorm` is the Euclidean continuous-linear-map norm. No scalar proxy, entropy identity, derivative, commutativity, eigenvalue separation or conditioning assumption enters the final theorem. The repeated/zero spectral cases and zero commutator are retained. The mathematical ID and target are unchanged.

The portable independent `audit_retained.py` strips nested comments, scans all 52 proof modules for proof gaps/trust escape constructs, reconstructs the actual complete Solution import closure, and checks all twenty source headers against Challenge. It found no proof `sorry`, `admit`, custom axiom, unsafe/native shortcut or Challenge import. The 20 deliberate placeholders occur solely in isolated Challenge. Every exported theorem is selected by Comparator with no replaceable definitions and only the three standard permitted axioms. The seven MI-24 support modules are byte-identical to the canonical published base. Ten resolved package revisions are fixed 40-character hashes; Lean is pinned to 4.33.1.

The same audit authenticates 41 compressed original receipts, 689 exact source/dependency/output reuse links, 52 current proof modules, all saved successful origin logs, non-overlapping compiler intervals, the aggregate 20 standard-axiom reports, and the 1,296,589-byte identical raw type/universe diagnostic logs. The original author's live compiler, dependency checkouts and oleans are unavailable here, so the local archive audit is a retained-evidence consistency check, not a fresh local Lean execution. The published historical audit scripts reference original private paths; a new portable immutable-source verification script is supplied with promotion evidence.

## Actual external execution

Upstream Actions run `35438172834` succeeded for this PR head. Its actual checkout is the PR merge commit `f21284e35e8156a9761c1f2ccd49d579f42fb69a`, with parents `71563f17926cd826a892c2bba0e294894ee57a5c` and `1f05b398013d44beb7d756cbfbbfd3e875c8deab`. The coordinator downloaded artifact `10583511156` and matched its API SHA-256 `50394e222707d250e7e1fc58dc80b8f6e59fb475b324a3414fd59f36e045b094`; I independently checked the retained archive digest and every one of its 284 recorded project input hashes against the audited head. The input list exactly covers that project's files.

I inspected the actual Comparator log: all twenty Solution exports report only `propext`, `Classical.choice`, and `Quot.sound`; all twenty targets are exported, default-kernel replay accepts, Comparator accepts, exit is zero. The result's source-lock checksum agrees with the repository. The actual controls passed: five Comparator match/mismatch/axiom regressions; three raw-kernel honest/invalid-proof/quotient-mismatch cases; build/export sandbox probes at non-root UID 1001 (private namespaces, no effective capabilities, no-new-privileges, denied external writes/symlink escape/network/AF_UNIX/nested namespace writes); and illegal-axiom rejection of separate sorry and native-decision fixtures with expected exit one.

## Promotion prepared

The isolated worktree updates canonical MI-27 README, RESOLVED, live Lean README/STATE/formalization metadata, retains official CI artifact/logs and sanitized GitHub metadata under `lean/verification/linux-2026-09-19`, and adds a portable evidence checker. Frozen historical snapshots, proof modules, definitions, Challenge, comparator configuration and dependency pins remain byte-identical. Documents accurately distinguish the actual PR merge checkout from its source-identical immutable head and state that later publication changes require separate CI. Analytic/formalization authorship and AI assistance remain explicit; no human peer-review, official Tau Ceti or priority claim is introduced.

The canonical TeX/PDF is regenerated with the repository renderer. The renderer labels MI-27 and NR-04 dates as verification checks, and removes MI-27's now-unnecessary forced references page break. No catalog/index regeneration was performed in this agent's worktree; the coordinator integrates promotion with other PRs and runs the required catalog/ID checks.

## Validations and records

- Original and promoted metadata validate against the repository v0.4 schema and match all 20 Comparator targets.
- Permanent-ID validation against `origin/main` passes; 17 permanent-ID regression tests pass.
- `retained-audit.json` / `.log` record independent archive checks.
- `ci-audit.json` / `.log` record exact source matching and control checks.
- Published `verify_evidence.py` passes for all 284 immutable inputs and live proof/contract/pin equality.
- Canonical PDF visual QA is performed after rendering; no fresh Lean run is claimed by this agent.

Remaining merge gate: source-unchanged publication/combined CI after promotion, to be run by the coordinating agent. This is routine execution validation, not an unresolved mathematical or source-review issue.
