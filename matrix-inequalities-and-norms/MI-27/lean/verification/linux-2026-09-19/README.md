# MI-27 Linux verification, 19 September 2026

[Upstream run 35438172834](https://github.com/ajt60gaibb/OpenProblemsInNLA/actions/runs/35438172834) actually checked PR merge checkout `f21284e35e8156a9761c1f2ccd49d579f42fb69a`. Every one of its 284 submitted project inputs is byte-identical to immutable PR head `1f05b398013d44beb7d756cbfbbfd3e875c8deab`. Artifact `10583511156`, retained as `lean-MI-27.zip`, has SHA-256 `50394e222707d250e7e1fc58dc80b8f6e59fb475b324a3414fd59f36e045b094`, independently matched to the GitHub artifact digest by the coordinating audit.

The original artifact contents are retained under `verify-20260919T104455Z-4157/`. `SOURCE-AUTHENTICATION.json` records independent hash, configuration, axiom and log checks. Actual Comparator matched all twenty concrete frozen contracts, Lean's default kernel accepted the exported proof, and all exports reported only `propext`, `Classical.choice` and `Quot.sound`. The five Comparator regressions, three raw-kernel replay cases, strict build/export sandbox probes at UID 1001, and rejection of sorry/native proofs passed. No source review is inferred from CI.

`RETAINED-EVIDENCE-AUDIT.json` separately records authentication of the submitted local evidence: 52 proof modules, 41 receipt archives, 689 exact-successful reuse edges, matching frozen source headers and byte-identical separate-environment types. It is an evidence consistency check, not a new local compiler execution. The two prior wholly nonauthor source reviews remain under `../../reviews/`.

The publication/status/evidence changes made after this run require their own subsequent CI. Proof modules, definitions, Challenge, comparator configuration, toolchain and dependency pins are unchanged. `verify_evidence.py` authenticates the archived result against the immutable Git source; it deliberately does not claim the earlier run checked later documentation. This promotion covers the original upper bound, not the manuscript's separate optimality result.
