# MI-24 independent full-source review — local325

**Verdict: request changes for proof quality and reuse. No mathematical or statement-faithfulness defect found in the complete source review.** This is a nonauthor Codex-agent review by `/root/mi24_full_referee1`, under the five supplied Tau Ceti rubrics at commit `afb424eda89e8ac96d9eb69f6a88972055a4cd1b`. It is not an official Tau Ceti review-engine run or external human review. I did not write any reviewed proof, run Lean/Lake/Comparator, change proof sources or Git, publish, or change either campaign count. I knew the author's local-success claim before reviewing and independently checked the retained evidence rather than treating that claim as semantic proof.

The exact subject is `/private/tmp/nla-lean-next-20260915/MI24-local325-source-snapshot`, manifest SHA-256 `68d81b42a0cd7fe090b31e53b4079cfdb3ca60f143eb82f09b78bbaa2d1969b5`. The local completion record is SHA-256 `499beaeef51d1abc09537258d14442085443ffe50f4f649205d6eb9846981ada`. This verdict applies only to these bytes. A successor containing fixes needs a bound continuation; this review does not authorize treating changed bytes as reviewed.

## Required source cleanups

1. **R1 — use the existing matrix-continuity result.** `candidate/NLA/MI24/DiagonalPowers.lean:27–38` reconstructs diagonal continuity entry by entry. Pinned Mathlib already provides `Continuous.matrix_diagonal` in `Mathlib/Topology/Instances/Matrix.lean:142`. Apply `continuous_id.matrix_diagonal` after explicitly exposing the underlying diagonal function, or use it directly at the sole consumer in `spectralPower_diagonal`. The statement may remain a useful bridge to the bundled homomorphism; the coordinate proof duplicates the located library fact. Any suggested Lean replacement is a source recommendation, not a compiler-tested patch by this reviewer.

2. **R2 — remove the duplicate star/adjoint alias.** `candidate/NLA/MI24/CompoundSpectral.lean:23–24` defines `compoundMatrix_conjTranspose` by forwarding directly to `compoundMatrix_star` from `CompoundAlgebra.lean:99`. The only consumer is the rewrite in `CompoundSpectral.lean:89`. Use the existing star lemma there, making the matrix-star/conjugate-transpose notation bridge explicit if needed, and remove the compatibility-only alias. This is an internal duplication request, not a claim that Mathlib already supplies the compound-norm theorem.

3. **Q1 — document deliberate definition/coercion exposure.** The pinned proof-quality rubric expressly requires a reason for `change`/`show` usage. The exact outstanding sites are listed in `BINDINGS.json` under `proof_quality_sites`, grouped by shared rationale. In particular, the Riccati proof's local abbreviations (`GeometricCongruence:54,78,85`), trace/entry coercions (`TraceOverlap:51,57,91`), and finite-subset/order coercions (`CompoundNorm:71,73,90,95,105,109`) currently rely on unexplained definitional equality. Prefer controlled unfolding or an existing explicit bridge when available; otherwise add a nearby precise explanation of what is being exposed and why. The typed arithmetic `show` terms are proved identities, not semantic defects; their purpose is to pin the real or complex expression being rewritten. A coherent block may share a rationale. `CompoundAlgebra:69–70` already has a determinant-coordinate explanation and is excluded from this request.

These findings concern maintainability and the supplied review standard. They do not identify a false theorem, hidden mathematical assumption, unproved prerequisite, or forbidden axiom.

## Statement and mathematical assessment

I read every line of all **34 local source-closure modules** (including `Definitions.lean` and `Solution.lean`), the complete frozen `Challenge.lean`, all four build/Comparator configuration files, the author navigation guide, scalar pre-proof statement, scope/freeze records, and the capture script. `BINDINGS.json` enumerates every source, its exact hash, and its full line coverage. The author guide was navigation only. The two prior statement approval manifests were authenticated; I did not read another final proof referee's verdict or borrow it as evidence.

I compared the target with the retained canonical README and full solution at upstream commit `e7519c46fd249a6a033bfe5d11c66bf47f7f8885`, and checked the retained primary expositions: Dinh–Dumitru–Franco's Theorem 4 and the weighted-trace proof route, plus Fujii's Lemma 1.2 and Theorem 1.3 base/extension argument. These are retained-source reads, not a new live upstream or literature search. No new mathematical-resolution claim is made.

- **Full original target.** `Mat n` is the square complex matrix space on `Fin n`; C22 quantifies over every `n ≥ 1`, every positive definite `A,B`, every real `p ≥ 1`, and the infinity endpoint. The Lin quantity has `B^(1/2) A^(-1) B^(1/2)` inside the square root and `A^(1/2)` outside. No interchange of A and B or symmetry of Lin's quantity is assumed. C02 ties the negative power to the genuine nonsingular inverse. There is no commutation assumption, dimension cap, finite exponent list, mean/norm axiom structure, or conclusion hidden in a final hypothesis.

- **C01–C06.** The two LeanCert obligations are exact affine bounds on a single interval. The implemented certificate is actually called at `youngWeight p = 1/(2p)`; C09 obtains complement-weight nonnegativity from it. The geometric mean is the actual ordered CFC expression. Its Riccati equation and positive square-root uniqueness prove invertible congruence. Positivity of all eight concrete means/sums is proved, including the identity of the Heron cross sum with `(A^(1/2)+B^(1/2))²`. The half-power fixed point is verified through the two explicit summands `(A+G)/2` and `(B+G)/2`, not a general existence oracle.

- **C07.** `PolarPowers` constructs the polar unitary from `W(W*W)^(-1/2)`, proves its unitary property and reconstruction, and derives the real-power conjugation identity through Mathlib CFC naturality. `furuta_base` proves the first sandwich range by Löwner–Heinz, negative-power order reversal and congruence; it does not illegally square a matrix order. Its scalar exponent `(1-s)/(s+t)` is proved to lie in `[-1,0]`. `furuta_grade_two` covers `R ∈ [0,2]`: for `R > 1`, it uses `A₁=A²`, `B₁=(A^(1/2)B²A^(1/2))^(2/3)` and the proved base case at `s=3/2`. C07 takes `R=2r` and the additional exponent `(2+R)/(2(1+R)) ∈ [0,1]`, giving exactly the frozen square-root conclusion, including `r=0,1`. This is a complete internal proof of the bounded slice needed later.

- **C08.** `HeronOrder` proves the genuine implication `T ≤ I → S ≤ I` with `r=1/(2p-1) ∈ [0,1]`, including `p=1`. `HeronNorm` scales the arbitrary input D by the square of the reciprocal of the target norm; that denominator is proved positive from positive dimension and positive definiteness. `CompoundAlgebra` uses actual minors, exterior-basis coordinates, and multiplicativity. The compound diagonalization, positivity, and CFC power transport hold at all degrees; `k ≤ n` proves the compound dimension positive. `CompoundNorm` explicitly relates the chosen eigenbasis indices to Mathlib's descending spectrum, bounds each sorted-subset product by the leading product, and constructs an attaining subset. No unsorted eigenbasis is silently treated as sorted. The empty-product degree `k=0` is included. Applying the norm implication at every `k ≤ n` yields the required prefix-product inequalities.

- **Scalar transfer.** I independently checked the root-authored `ScalarLogMajorization.lean`. Positivity permits taking logs of all prefix products. The tangent inequality is in the correct direction, `a-b ≤ a(log a-log b)`. The induction subtracts the final nonnegative weight and uses nonpositive prefix sums, yielding the finite Abel inequality for antitone source weights. This suffices even without sorting the target vector. Both `n=0` and `k=0` are covered. `WeightedTrace` then identifies both sides by explicit cyclic trace algebra. It neither assumes a matrix log-majorization theorem nor postulates the weighted trace comparison.

- **C09–C10 and certificate consumption.** The two-basis trace expansion uses actual squared moduli of unitary entries. Both marginals are proved to sum to one, and every weight is nonnegative. Scalar weighted AM–GM consumes C01 through `youngWeight_complement_bounds`; summation gives C09. The proved fixed point and Q square-root identity give C10 after cancelling a strictly positive Young weight. The source dependency is C01 → weight bounds → scalar Young → C09 → C10 → C17/C18 → C20/C21 → C22. No interval subdivision, matrix-entry enumeration, or numerical eigenvalue calculation is used.

- **C11–C16, including degenerate cases.** PSD finite-p monotonicity and the triangle inequality use an explicitly proved trace Hölder inequality. The overlap-weight splitting treats zero weights and zero eigenvalues with nonnegative-exponent identities. `p=1` is a separate trace-linear case; `p>1` uses the conjugate exponent. Zero trace is handled before cancellation. Homogeneity includes scalar zero and singular PSD matrices. C15 concerns arbitrary complex X: the Gram spectrum and actual singular values are matched with multiplicity, and nonnegative power composition handles a singular Gram matrix. C16 uses the genuine `Matrix.toEuclideanCLM` norm and the largest singular value, not an entrywise or function-space norm. The scoped L2 operator norm is tied back to the frozen Euclidean definition by Mathlib's explicit bridge.

- **C17–C22.** The finite Heron comparison follows for every real `p ≥ 1` from C10 and genuine positive homogeneity. Infinity is proved by a finite natural-power contradiction: `‖U‖^m ≤ tr(U^m) ≤ n‖U‖^m`, with a positive denominator, a norm ratio greater than one, and a proved `m ≥ 1`; no unproved Schatten-limit result is assumed. The polar-modulus comparison produces precisely the ordered Lin/geometric expressions after congruence. Finally, the literal midpoint identity, the Heron/Lin inequalities, PSD monotonicity, and the actual triangle inequalities give the two original conclusions and their conjunction. The use of PSD-only intermediate norm results does not narrow the original PD inputs because positivity is independently supplied.

Adversarial cases checked during this read include `n=1`, `A=B`, complex noncommuting inputs, repeated eigenvalues, arbitrarily small positive eigenvalues, all finite real exponents, `p=1`, infinity, zero/singular matrices in the PSD and norm-semantics contracts, scalar zero, and compound degree zero. The statement predicate has the nontrivial consuming C22 theorem; it is not an unexercised abstract predicate or a vacuous `True` proxy.

## Rubric results and limits of the reuse search

Correctness/faithfulness: **approve at source scope**. Generality: **approve for the original finite-dimensional target**. Proof quality: **request changes (Q1)**. Reuse: **request changes (R1, R2)**. Attribution: **approve at source scope**.

The preserved frozen contracts contain some logically unnecessary dimension or positivity premises, e.g. C14's `hV` follows from `0 ≤ U ≤ V`. Their exact source boundary is a task constraint and they do not narrow the canonical result. Nonfrozen helper statements already extend to PSD matrices, zero dimension, general real powers, or arbitrary vectors where the argument uses that generality. I do not request changing the statement freeze to silence unused-variable warnings.

The reuse inspection covered the pinned local Mathlib and LeanCert sources and available campaign source exemplars, with targeted content searches for real-power/CFC identities, square-root uniqueness, order, polar formulas, diagonal continuity, exterior powers, sorted spectra, singular values, trace overlaps/Hölder, and logarithmic prefix summation. Search commands and outputs are retained in `BINDINGS.json`. `Finset.sum_range_by_parts` was located as a related ℕ-range identity, but it is not a direct replacement for the signed `Fin n` prefix inequality proved here. No already implemented Furuta, Schatten, or full compound-norm API directly replacing the substantive work was located in this scope. Only the TauCetiReview guidance checkout was available, so **no exhaustive Tau Ceti mathematical-code duplication search is claimed**.

George Stepaniants is credited with the Department of Computing and Mathematical Sciences, California Institute of Technology affiliation. The headers preserve Ghabries–Abbas–Mourad–Assi's original question, Dinh–Dumitru–Franco's Heron comparison, Fujii's Furuta exposition, and the adapted MI22/MF06/MF05 code lineage. The scalar helper and other proof author's separate agent roles are explicit. No email address is included in this review.

## Actual evidence checked

`verify_review.py` authenticates all 46 snapshot manifest entries, every reviewed local source, all 22 frozen source headers, the freeze and two prior statement review manifests, the retained local completion record, all **25 receipt files and 34 successful-origin logs**, all historical assembly bindings, the exact serial compiler arguments, the transitive source matches through reuse, and each direct dependency output-hash edge. It parses the actual final Solution log and requires all 22 configured names with only `propext`, `Classical.choice`, and `Quot.sound`. It verifies the 22 source `#assert_trust kernel` commands and authenticates the pinned LeanCert implementation that rejects native, sorry, and custom axioms.

The authenticated compiler receipts are **root's actual macOS Lean runs**, using one compiler process/thread and `--memory=4096`, with Numerical's successful origin at local293 and Solution's at local325. The final run reused source-matched successful outputs; it did not freshly compile all 34 MI-24 modules. Local325 also selected unrelated MF14 modules; the MI-24 claim is restricted to its exact 34-module closure. Compiler logs contain some warnings, including LeanCert-internal tactic diagnostics, but their bound successful origins exit zero. The final all-22 log contains the expected axiom reports and no errors. I did not repeat any of these Lean commands.

There is one **nonblocking metadata typo** in the immutable completion record: `source_filename_mapping.local` says `NLA.MI24.Solution.lean`, while the authenticated assembly and compiler argv correctly use `NLA/MI24/Solution.lean`. Their hashes agree with canonical `Solution.lean`, so the real source binding is unambiguous. The checker explicitly discloses and tests this discrepancy rather than silently treating the display field as a filesystem path. Correct the capture-script mapping in the successor record and preserve local325.

The review replay command is `python3 /private/tmp/nla-lean-next-20260915/reviews/MI24-final-referee1-local325/verify_review.py`. Its actual command, exit code, complete stdout, checker hash, and sealed-manifest hash are recorded in the sibling `MI24-final-referee1-local325-CHECK.json`, outside the sealed manifest to avoid a self-hash cycle. This is an integrity replay, **not a Lean compilation or kernel rerun**. Current mutable `.olean` files are not archived by this review; output hashes are authenticated through the retained source-matched command/dependency records. Shared dependency caches were not independently rebuilt.

Actual elaborated-type comparison, final real non-root Linux GitHub Comparator/kernel/sandbox checks, final publication metadata including truthful `formalization.yaml`, setting the final Lake default target to Solution, and publication/count acceptance remain separate gates. The historical draft notices and current Challenge default target are explicitly outside a final-packaging approval. This review approves no publication and increments neither the mathematical-resolution nor complete-Lean-verification count.
