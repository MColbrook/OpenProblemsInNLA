# MI-28 Linux verification, 2026-09-18 (UTC)

Actual [run 35374928604](https://github.com/sgstepaniants/OpenProblemsInNLA/actions/runs/35374928604/job/105697400237) checked literal proof commit `aae2941f9a7f1e28b8010f5216574d93bdc72181`.
The retained GitHub metadata, original artifact ZIP and complete logs bind all
282 tracked project inputs and all 20 exports. Real Comparator statement matching,
Lean default-kernel replay, permitted transitive axioms, sandbox checks and
negative/regression controls passed on the non-root Linux runner (UID 1001).
Only MI-28 was selected. Its job ran all per-project controls; the separate
global checker-controls job was skipped because shared tools were unchanged.

`ROOT-AUDIT.json` records the coordinator's inspection of this actual execution.
It is not a second compiler run or an independent mathematical referee report.
Two nonauthor full-source approvals and a separate package review are indexed
in ../../REVIEW-INDEX.json. Actual macOS local365 compilation, local366 type/body
diagnostics, and the fresh canonical direct-Lean entrypoint are separate records.
Only Challenge has 20 deliberate specification placeholders; Solution never
imports it. Every export uses only the three standard foundational axioms.

This execution covers the immutable proof revision. Later publication files
and the upstream PR merge checkout require their separate runs, recorded in the
PR. An earlier run does not check future files. The retained evidence does not
assert that Lean, the harness or GitHub is infallible.
