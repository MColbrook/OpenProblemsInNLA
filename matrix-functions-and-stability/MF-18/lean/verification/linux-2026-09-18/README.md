# MF-18 Linux verification, 2026-09-18 (UTC)

Actual [run 35315336123](https://github.com/sgstepaniants/OpenProblemsInNLA/actions/runs/35315336123/job/105505732924) checked literal proof commit `0d3a658789510d3cdd14729f22165219f7e7eaf8`.
Retained GitHub metadata, the original artifact ZIP and complete logs bind all
226 tracked project inputs and all 25 exports. Real Comparator statement matching,
Lean default-kernel replay, permitted transitive axioms, sandbox checks and
negative/regression controls passed on the non-root Linux runner (UID 1001).
Only MF-18 was selected. Its verification job executed all per-project controls;
the separate global checker-controls job was skipped because shared tools were unchanged.

`ROOT-AUDIT.json` records the coordinator's inspection of that actual execution.
It is not a second compiler run or an independent mathematical referee report.
The two nonauthor full-source approvals are in ../../reviews/.
Actual macOS local340 compilation and local341 type/body diagnostics are retained
separately. Only Challenge has 25 deliberate specification placeholders;
Solution never imports it. Every export uses only the three standard axioms.

This result covers the immutable proof revision. Later publication files and the
upstream PR merge checkout require separate runs, recorded in the PR. An earlier
run is never represented as checking future files. Hash and log checks establish
the recorded execution, not the infallibility of Lean, the harness or GitHub.
