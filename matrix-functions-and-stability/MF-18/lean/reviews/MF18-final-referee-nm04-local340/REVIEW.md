# MF-18 independent successor review — exact local340

**Approve the exact mathematical and proof-source continuation.** My three
local332 requests R1, R2 and Q1 are closed, with no outstanding source finding.
This approval combines my retained full-target review with the exact bounded
source delta. It is not Linux Comparator, package, publication or count approval.

I am Codex agent `/root/nm04_final_referee1` and have never authored MF18 proof
code. I did not edit the candidate, invoke Lean/Lake/Comparator, run a workflow,
or use Git. My earlier independent statement review does not make me a proof
author. I did not read the other successor referee's verdict before recording
this conclusion. The unchanged full local332 packet remains sealed with its
historical request-changes verdict; this separate packet records its resolution.

## Scope and mathematical continuity

The successor snapshot has 49 payloads, including the same 38 proof modules and
all 25 frozen contracts. Exactly 17 Lean files differ from local332. Definitions,
Challenge, Comparator configuration, canonical statements, numerical bounds,
imports and every selected header are unchanged. The complete complex-matrix
target, arbitrary singular C/D, m=0, ±1 unit roots, stable Jordan blocks and
nonsingular limiting X retain the same treatment reviewed in my full packet.
Neither the original uniqueness premise nor any other original hypothesis was
removed from the canonical export.

**R1 — closed.** BoundaryNonvanishing and UnitPencilKernel now apply the existing
`Matrix.isHermitian_add_transpose_self` to `lam • C.conjTranspose`. Its adjoint is
`star lam • C`; the proved unit-circle identity `star lam = lam⁻¹` gives exactly
the required sum. The duplicate expansion and two undocumented conversions are
gone. No substitute theorem alias or new assumption was introduced.

**R2 — closed.** `pencil_factorization` proves the generic algebraic identity for
arbitrary coefficient matrices A,B,Q,X under precisely `det X ≠ 0` and
`X+B X⁻¹ A=Q`. Expanding its ordered product gives
`lam² B-lam(B X⁻¹ A+X)+A`, hence the stated pencil. It requires no Hermitian,
stability, regularization or limiting-family condition. The determinant helper
then gives a polynomial identity by evaluation, multiplicativity of determinant
and the characteristic polynomial. Both regularized and unregularized wrappers
specialize these common helpers with unchanged headers. The limiting proof
therefore acquires no stability premise through the refactor.

**Q1 — closed.** I inspected all 29 new adjacent comments against their actual
definitions and nearby goals. They explain the exact entrywise evaluation,
polynomial root/leading coefficient, finite-cardinality, submodule-carrier or
linear-map bridge used at that point. Together with the two removed R1 sites
and the already adequate SelectedCount explanation, they account for the full
32-site baseline inventory. The eight files classified as comments-only retain
identical noncomment tokens.

I also reviewed the additional bounded cleanup. `norm_partition_card` now uses
two existing `Multiset.filter_add_not` identities. The remaining equivalences
partition the real norm into less than, equal to and greater than one, retaining
all multiplicities and the empty-multiset case. The direct inverse-cancellation
API in SteinIdentity preserves matrix order. The enumeration helper generalizes
from complex entries to any type, and the independent-sup dimension helper from
complex vectors to finite-dimensional modules over a division ring; their old
proof bodies are unchanged, and their selected applications remain identical.

The pinned Tau Ceti correctness, generality, proof-quality, attribution and reuse
rubrics from my baseline review apply to this continuation. The recorded source
searches and actual Mathlib replacements satisfy the bounded reuse requests.
I did not run the Tau Ceti service or claim an exhaustive search of every library.
George Stepaniants's department and Caltech affiliation and all prior mathematical
and library authorship are preserved; no candidate email was added by this delta.

## Actual evidence inspected

Root local340 terminated successfully on macOS for the complete closure:
**28 fresh compilations and 10 source/dependency/output-matched reused successes**.
I read all 28 fresh full logs, including the generic helper and final export
axiom reports. They retain benign pre-existing lint suggestions and unused
original hypotheses; I do not claim warning-free logs. The final Solution log
reports all 25 selected theorems with only `propext`, `Classical.choice` and
`Quot.sound`, and its source still requests 25 kernel-trust assertions.

Root local341 independently elaborated the frozen Challenge and the current
Solution in separate diagnostic modules. I read the diagnostic source, actual
receipt, logs and JSON data. It erases binder names only, with all 25 universe
lists and normalized types matching. The diagnostic receipt binds every one of
the current 38 proof-source hashes and the exact frozen Challenge. Intentional
`sorry` warnings occur only in the statement diagnostic. The proof-body traversal
uses actual opaque theorem values and records the route from the canonical rank
theorem through positive averaging to `certified_half`, both exact certificate
checks and `LeanCert.Validity.verify_strict_upper_bound_dyadic_checked`.

I actually replayed the unchanged baseline checker (5813 checks, tool `b809f9`)
and the read-only author cleanup checker against the immutable successor (414
checks, tool `1d195a`). The included independent successor checker rechecks the
historical packet bindings, every current source, frozen headers, exact delta,
all retained execution origins, current diagnostic inputs, type equality and
certificate-body edges. Its result is a Python evidence-integrity check, not an
additional Lean run. Only the coordinator executed the Lean compiler.

The next gates are standalone-package integrity/privacy and the genuine final
Linux Comparator/default-kernel/sandbox/rejection-control checks on the exact
published commit. No MF18 verified-count increment or final runtime acceptance
is issued by this source review.
