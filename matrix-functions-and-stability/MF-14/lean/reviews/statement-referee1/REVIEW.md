# MF-14 degree-44 alternative: independent statement review

Reviewer: `/root/pf03_final_referee2`, independent nonauthor AI agent. Phase: statement and transparent-definition review before substantive proof implementation.

**Verdict: APPROVE the exact elaborated v2 statements for subsequent proof development.** No mathematical header correction is requested. This is a statement approval, not acceptance of an implemented proof. No Lean compiler or Comparator was run by this reviewer, and no proof body or target was modified.

## Sources and exact scope

I read the retained canonical MF-14 question, Marcus Webb's primary degree-44 `proof.tex`, the preceding route assessment, all four definition/data modules, the complete 25-statement Challenge, Comparator configuration, numerical-target plan, and preparation/source records. The primary proof SHA-256 is `c987a6ac3532b50a8241702612c05d951d2a7d9b467a14aec922ea3c83d3de88`. I matched both that source and the canonical README to immutable Git blobs at `e7519c46fd249a6a033bfe5d11c66bf47f7f8885`.

The approved packet is `development/MF14-degree44-statements-v2`, sealed by `STAGING-HANDOFF.json`, SHA-256 `08bf0681f405454e4725e7dfaaaae279a1582abd1c698c2e7bc6e377cc5db3d1`. Its Challenge SHA-256 is `caba1fdbcc389d22c7353e26a2fb56d089992e4d5603f02b0e2f9ad1454d79e8`, and its new Definitions SHA-256 is `cb5cb2a374e2bc0a82e4cc0770a3e8e1baf7972f92ce17f51768a2d3ae7ea2f2`. The initial v1 seal, SHA-256 `54a2da95eeaf2c88d8f79cc5323a1de28ccff643987effd02c3eed66223acc9a`, is preserved for comparison.

`STATIC-AUDIT.json` binds every initially reviewed packet file and the source inputs; `V2-HEADER-AUDIT.json` binds the exact approved revision, its import-only delta, and the successful root-run evidence. The independent audit scripts check these bindings and bounded data correspondence; they run no compiler and prove no Lean theorem. The original `NLA/MF14/Definitions.lean` is byte-identical to the shared file, SHA-256 `f9ab23794894bfa508b5382393d95457a361cf07063629c7f48e5f14fefea9a8`. The existing degree-47 Challenge remains separately bound to SHA-256 `8d1bb0877a3de9cb11db53f7b3d8985b50eb2b972a28b5bede7029f8277f0985`.

The unchanged original question is whether 42 is the greatest degree whose entire complex coefficient plane belongs to the seven-product Zariski closure. The proposed final statement is exactly `¬ IsGreatest NLA.MF14.coveredDegrees 42`. Its preceding contract is the unconditional `NLA.MF14.CoversDegree 44`. Since 44 is within the original bound 128, proving that contract supplies a larger covered degree and refutes the original equality. A single computable polynomial of degree 44 or 128 would not suffice; this packet demands the whole plane, including zero and all smaller degrees.

This alternative does **not** discharge the separate exact-maximum-47 contracts or certify the later upper-bound argument. Those sources and their pending status must remain distinct. A future completed alternative would count the original MF-14 target once, not as a second resolution or as proof of exact maximum 47.

## Circuit and closure fidelity

`SimultaneouslyAvailable` existentially chooses one common `GatePolynomials` value and one chronological prefix. Each output belongs to that same prefix's available span. Unused future slots are excluded by the strict index bound in the unchanged `availableGenerators`. Thus the four outputs are not allowed to come from four independent circuits. `canonical_gate_padding` separately requires the at-most-seven/exact-seven equivalence, including the zero-product case.

`QuadSpace` is the full `Fin 4 × Fin 17 → ℂ` space. `four_prefix_full_coefficients` requires both the actual degree bound and exact decoding; the coordinate definition cannot silently erase higher coefficients of a genuine prefix output. `jointFourClosure` uses all polynomial equations on those 68 coordinates. It is not a closure in a projected output coordinate, and no density or coverage theorem is concealed in its definition.

The three-product realization is true with the stated single nonzero-denominator premise: `gamma*s - 2*s² ≠ 0` already forces `s ≠ 0`. Expanding the primary factorization gives the required `Rparam`; its correction terms use only freely available earlier polynomials. `good_border_parameters_eventually` is quantified over every complex `alpha,eta,gamma`, including `gamma=0`; for that case the denominator is `-2*s²`. The minor's bracket tends to one, so no parameter exception needs to survive into the final border result.

The selected 12 coefficient rows are legitimate analysis coordinates only after `fourth_span_coordinates` is proved. Its bijectivity clause addresses the full product span, whose listed basis has distinct leading degrees 0 through 6, 8, 9, 10, 12, and 16 when `s ≠ 0`. The fourth-product map uses exactly one new product plus a free sum from the same three-product prefix. Its 12 derivative directions and row order agree with the source. The minor is required as an exact identity for all complex parameters, including singular specializations.

The degeneration uses `lowPart6` only inside the proved vector space, not as a new circuit primitive. The syzygy identity, value at zero, and span membership are explicit contracts. I matched all five literal cofactor expressions to the primary symbolic certificate and checked the signs and powers in `borderC`, `borderD`, and `degenerationZ` against the primary appendix. These are transparent data, not imported certificate verdicts.

Restricting the border theorem to the stated monic degree-12 family is adequate: it is exactly the family used by the subsequent 45-parameter continuation. Choosing a complex square root fixes its coefficient of degree 11, and the limiting triangular monic basis supplies all coefficients through degree 10. The theorem remains unconditional in all complex `alpha,beta,xi`. The unused general leading-coefficient-zero case from the stronger source lemma need not be formalized to obtain this result.

## Continuation, derivative, and density obligations

The continuation has three actual multiplication stages after the common four-product prefix. Its parameter blocks are 9–12, 13–15, 16–20, 21–23, 24–29, 30–35, and 36–44, with the border parameters at 0–8. Explicit bounds prevent indexing wraparound. I checked these blocks and the base point's six unit entries at indices 0, 22, 32, 34, 35, and 44 against the primary source.

For arbitrary decoded joint inputs, the three products have degrees at most 32, 48, and 96. `continuation_degree_bound` and `continuation_closure_transfer` preserve the full original `Fin 129 → ℂ` coefficient vector. At the special border family the degrees are at most 17, 22, and 44; `family_degree_and_full_vector` explicitly requires all coordinates above 44 to vanish. They are not discarded during the ambient closure argument.

The compact integer derivative table matches the source's chain-rule formulas. The independent audit evaluated this literal table and matched all 2,025 entries against the archived integer matrix; that is only source/data correspondence. `coefficient_jacobian_identity` independently demands equality with the actual `fderiv` of the actual 45-parameter map. The final nonsingularity theorem also concerns that actual derivative. Consequently a stored invertible matrix alone cannot discharge the required bridge.

The modular inverse statement is a concrete existential certificate obligation, not an inverse assumed as input. Once proved, determinant naturality modulo 3 implies the integer determinant is nonzero and hence its complex image is nonzero. Computing the exact value 256 is unnecessary for the consumed claim. The stated plan avoids factorial determinant expansion and allows small exact kernel-checked partitions.

The generic strict-derivative density contract is mathematically sound without a global polynomial-map hypothesis: an invertible strict derivative supplies a neighborhood in the image; a complex polynomial vanishing there is identically zero. The `n=0` case reduces to constants and remains valid. This is polynomial-equation density, not the invalid claim that an arbitrary Euclidean-open image is already the whole plane. Both final coverage contracts have no invertibility, genericity, nonzero coefficient, or numerical condition left as a hypothesis.

## Review standards and remaining gates

The original header attempt lacked the explicit import defining `InnerProductSpace.toNormedSpace`. The only source change in v2 is `import Mathlib.Analysis.InnerProductSpace.Defs`; I verified that removing exactly that line recovers the original file byte for byte. All mathematical definition bodies and all Challenge statements are unchanged.

I inspected the root agent's `recovery-057` receipt, SHA-256 `820ceae2ddc164184dcc45b3253002eca3a4489d730dcc4149352764accbfb5c`, and checked its source, output, dependency-output, and raw-log hashes. Four new files were actually compiled with exit zero, one thread, and a 4096 MiB limit. The unchanged original Definitions output was reused, and I traced that reuse to an actual successful original command with matching hashes. The Challenge was staged under the filename `MF14Degree44Challenge.lean` with identical bytes; its raw log contains exactly the 25 expected placeholder warnings. There were no failed or blocked modules. This establishes statement elaboration only, not proof verification or an independent rerun by this referee.

I also accept the separate source-only API plan and `LEAN_CERT_ROLE.md` clarification, bound by their manifest. I checked its key inverse-function, polynomial-extensionality, and LeanCert trust-audit APIs directly in pinned sources and matched all 17 referenced Mathlib/LeanCert source files to their pinned Git objects. The proposed use of `#assert_trust kernel theoremName` genuinely traverses axiom dependencies and rejects unsafe/custom axioms; setting an option or importing LeanCert alone would not suffice. There is no interval obligation in this exact route, so the plan correctly avoids inventing one. The API plan remains unimplemented; it is not evidence that any trust assertion or density proof has run.

I applied the repository's pinned Tau Ceti adaptation (`afb424eda89e8ac96d9eb69f6a88972055a4cd1b`) as recorded in `docs/lean/REVIEW.md`: original-target fidelity, quantifiers and degeneracies, explicit certificate bridges, computation reduction, reuse of transparent definitions, and attribution. This is not a Tau Ceti service run or human peer review. The new namespace preserves the existing target API and separates the 44- and 47-degree families. Marcus Webb's mathematics, Matthew Colbrook's earlier degree-42 result, and George Stepaniants's Department of Computing and Mathematical Sciences / California Institute of Technology formalization credit are retained. No personal email was found in the Lean sources.

All 25 proposed theorem names match Comparator's explicit list. There are no permitted definition holes; the axiom allowlist is only `propext`, `Classical.choice`, and `Quot.sound`. The intentional 25 `sorry` bodies occur only in Challenge. There is no Solution, and the definition modules contain no asserted mathematical lemmas or custom axioms.

Approval of these statements does not verify the border construction, limit argument, IFT bridge, actual derivative, arithmetic certificates, or final theorem. Those remain substantive proof obligations, followed by local checking, independent final proof review, and the actual GitHub Linux Comparator gates. The LeanCert kernel-only plan is recorded but has not been executed for this packet. Neither a statement approval nor header elaboration increases any verified-problem count.
