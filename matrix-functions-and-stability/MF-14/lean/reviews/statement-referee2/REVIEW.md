# MF-14 degree-44 independent statement review

**Verdict: PASS for the exact v2 definitions, 25 theorem signatures, and Comparator mapping.** This approves the proposed mathematical statements for subsequent proof development. It does not certify a proof, a LeanCert result, a Comparator run, or a completed canonical target.

Reviewer: `/root/mf14_degree44_statement_referee3`, an AI agent and nonauthor of this packet, its import repair, and its future proofs. Phase: independent statement review, 19 September 2026. I did not read the other current statement referee's report or use its verdict. I changed no sealed input and ran no Lean compiler or Comparator. The bounded checks I performed were file/hash, literal-data, header-name, and existing-log inspection; I did not rerun a mathematical certificate verifier.

The reviewed packet is `.local-recovery-20260918/development/MF14-degree44-statements-v2`. Its `STAGING-HANDOFF.json` SHA256 is `08bf0681f405454e4725e7dfaaaae279a1582abd1c698c2e7bc6e377cc5db3d1`. `MANIFEST.json` records reviewed inputs by scope; `BINDING-AUDIT.json` records the reproducible byte/log checks and all 25 names.

## Original target and statement fidelity

I read the canonical MF-14 README at the path given by the published `problem_ids.json`, the unchanged original `NLA/MF14/Definitions.lean`, the complete primary degree-44 `proof.tex`, all three new definition modules, every Challenge signature, the numerical plan, and the Comparator configuration. The original question is complex Zariski closure coverage in the full coefficient space through degree 128, under at most seven chronological products and free complex linear combinations.

`degree44_coverage` concludes the unconditional original `NLA.MF14.CoversDegree 44`: **every** vector in the full degree-at-most-44 plane lies in the original seven-product closure. This includes zero and every lower degree. Consequently 44 belongs to `coveredDegrees`, since 44 ≤ 128; `IsGreatest coveredDegrees 42` would imply 44 ≤ 42. Thus `original_equality_false` is a complete negative answer to the unchanged original equality. A single high-degree output, a projected image, or a local open image alone would not suffice; none replaces this final contract.

The original definitions have SHA256 `f9ab23794894bfa508b5382393d95457a361cf07063629c7f48e5f14fefea9a8`, independently matched against the earlier degree-47 freeze. The old Challenge and Comparator retain their frozen hashes and 27 names. Exact maximum 47, the manuscript's auxiliary upper bound, exact representation, real density, and stable coefficient recovery are outside this separate gate. This review supplies no new mathematical resolution and no completed Lean verification.

## Adversarial checks of the proposed contracts

| Contracts | Statement-level assessment |
| --- | --- |
| D44-01a–b | The padding equivalence explicitly relates the original at-most-seven convention to seven slots. `SimultaneouslyAvailable` quantifies one common chronological circuit, not a circuit for each output. `four_prefix_full_coefficients` requires degree ≤16 **and** exact decode/reconstruction of every actual four-product tuple. The literal ambient index is `Fin 4 × Fin 17`, so all 68 coordinates are retained. |
| D44-02a–b | The third-product tuple is shared and uses the manuscript's `R_s`. Its denominator condition is exactly `gamma*s - 2*s^2 ≠ 0`. Eventual goodness is required for every complex alpha, eta, gamma; at lambda = eta+1 the minor is `s^10` times a polynomial equal to 1 at zero. Even gamma = 0 presents no exceptional final case. |
| D44-03a–e | The twelve affine directions differentiate to the twelve displayed columns in order. The selected rows are precisely 0,1,2,3,4,5,6,8,9,10,12,16. Their restriction must be bijective on the **entire** product span, whose basis has those leading degrees when s ≠ 0. Subtracting `a*column8 + column6 + (b-a*lambda)*column7` from column5 leaves `(eta-lambda)X^3 - (b-a*lambda)*lambda*X^4`; the resulting 2×2 determinant gives the stated minor and sign. The joint-closure conclusion retains all earlier polynomials. |
| D44-04 | A strict derivative that is a continuous linear equivalence gives a nonempty open subset in the range. A complex multivariate polynomial vanishing there is zero, so no global polynomial-map assumption on f is needed. The statement also handles n = 0: the domain is a singleton and the polynomial ring contains only constants. This is polynomial density, not a false assertion that the image equals the ambient space. |
| D44-05a–c | All five syzygy expressions match the archived source literals exactly. Their basis order agrees with the determinant's last-row order. `borderC`, `borderD`, and all six coefficients of `degenerationZ` agree with the manuscript appendix, including signs and s-powers. The exact identity requires coefficients 7–10 to vanish; degrees 11–16 remain explicit. At zero the result is `X^12 + (3*alpha-4*gamma^2)X^11`. `lowPart6` is justified inside the product span by the separate span-membership obligation; it is not a circuit operation. |
| D44-06 | The monic subfamily is sufficient. For any alpha, beta, xi, choose eta = beta+alpha² and a complex gamma with gamma² = (3*alpha-xi11)/4. The monic target minus the limiting Z has degree ≤10 and is covered by the manuscript's limiting triangular basis. General nonmonic degree-12 border coverage is unnecessary for the displayed continuation. No root nonvanishing or other final exceptional-parameter premise is introduced. |
| D44-07a–d | The continuation uses the same four inputs and exactly three more products. On arbitrary decoded inputs of degree ≤16, the successive degree bounds are 32,48,96. The output map goes to all 129 canonical coordinates, and closure transfer is explicitly required there. The family-membership contract consumes the joint border closure without discarding high coefficients. |
| D44-08a–e | Parameter blocks are 0;1;2–8;9–12;13–15;16–20;21–23;24–29;30–35;36–44, matching all 45 manuscript parameters without overlap or wrapping. Ones at 0,22,32,34,35,44 give alpha=b2=d3=d5=d6=z8=1. All 45 coefficient rows and derivative columns are retained. Degree ≤44 and full-vector equality with `embed44` are required. The actual derivative/table identity, modular right-inverse witness, and actual derivative bijectivity are separate obligations. |
| D44-09a–b | Both complete conclusions are unconditional and use the unchanged original predicates. No determinant, density, circuit correctness, supplied Boolean, or final coverage assertion is hidden as an input assumption. |

I independently checked the derivative table by the product rule at the stated base point. With B=R+X², K=2G+Q+P+F, and L=KB+G, perturbing F contributes L times that perturbation; perturbing P also contributes G times it. This yields the alpha, beta, xi, u, v, a, b, c, d, and z blocks displayed in `integerDerivativeColumns`. This is a mathematical statement check, not an entrywise Lean verification. A right inverse modulo 3 suffices to force the integer determinant, and hence its complex cast, to be nonzero; the unused stronger value 256 need not be a formal contract.

## Recorded elaboration and remaining scope

I verified the exact `HEADER-CHECK.json` binding to `recovery-057/RECEIPT.json`, SHA256 `820ceae2ddc164184dcc45b3253002eca3a4489d730dcc4149352764accbfb5c`. The raw `Challenge.lean` bytes match the renamed local `MF14Degree44Challenge` source in the receipt. The recorded four fresh commands use Lean 4.33.1, `--threads=1 --memory=4096`, and exit 0. All four log hashes match: definition logs are empty and the Challenge log contains exactly 25 deliberate `sorry` warnings. The shared original definitions were reused; I traced their source/output-matched receipt chain to the successful `recovery-005` command. The only v1-to-v2 Lean change is the explicit `Mathlib.Analysis.InnerProductSpace.Defs` import. I inspected these recorded results; I did not execute them.

All 25 unique theorem names match `comparator.json` in order. Its axiom allowlist is exactly `propext`, `Classical.choice`, `Quot.sound`. There is no Solution in the packet. Every proof remains a future obligation. Final kernel/trust audits, independent proof reviews, and the real non-root Linux GitHub Comparator/sandbox run remain required, with separate evidence. The optional `LEAN_CERT_ROLE.md` appropriately describes pinned kernel-mode trust auditing for exact algebra/modular arithmetic without inventing an interval obligation; it records a plan, not an executed certificate.

I read the pinned Tau Ceti correctness, generality, proof-quality, reuse, attribution, scope, API, naming, placement, and documentation rubrics and shared protocol. Their local bytes match the archived SHA256/Git-blob records for commit `afb424eda89e8ac96d9eb69f6a88972055a4cd1b`; I also inspected the retained tree-provenance record. This is the repository's adapted independent review, not an official Tau Ceti run. NLA's permanent target and path rules govern; Tau Ceti roadmap and compatibility policies are not imported. A bounded pinned-Mathlib source search confirmed reusable `HasStrictFDerivAt.toOpenPartialHomeomorph` / `map_nhds_eq_of_equiv` and `MvPolynomial.funext_set` ingredients. Exhaustive API and proof-quality review awaits actual proofs.

Credit is retained for Marcus Webb's degree-44 construction and Matthew J. Colbrook's earlier degree-42 result. George Stepaniants is credited for formalization preparation at the Department of Computing and Mathematical Sciences, California Institute of Technology; no personal email appears in the new package.

Nonblocking documentation note: the inherited README, NUMERICAL_TARGETS opening, REVIEW-REQUESTS risk list, and preparation metadata still contain pre-elaboration status language. The authoritative v2 handoff and HEADER-CHECK correctly record elaboration only. Preserve this sealed snapshot; update those status descriptions in a later publication package and bind any changed files separately. No mathematical statement change is requested.
