# Remaining publication gates

The complete original negative answer passes actual local serial Lean through
`recovery-095`, two independent full-source reviews, and two independent repair
continuations. All 25 implementation mappings match the frozen contracts and
sources. All 25 raw elaborated type and universe lists now match exactly.

The initial proof commit `fe4bbee26cc79f06587c803c5c902164c1e02e8e` passed
ordinary Lean compilation but failed Comparator's D44-08d statement comparison.
Its official failure records and independent blocking review are preserved.
The scoped instance-selection repair was diagnosed and tested locally.

1. Push the reviewed repair to the existing draft upstream PR305.
2. Inspect actual non-root Linux Comparator, default-kernel replay, permitted
   axioms, sandbox and negative controls on the repaired proof commit. Record
   official artifacts, hashes, commands, run IDs and any separate merge commit.
3. After those gates pass, update canonical verification documentation and
   indexes, preserving the original question, stronger exact47 mathematics and
   its authorship. Recheck the exact final published commit before requesting merge.

No completed-verification count increases while repaired Linux verification
is pending. The degree44 proof refutes the original equality42 question; it
does not formalize the later exact maximum47 theorem.
