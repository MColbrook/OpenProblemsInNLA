# Trace obstruction arithmetic: statement first

Frozen manuscript Section 5: the last paragraph after equations (32)–(33).

For every positive natural `k`, rational `u`, and nonzero rational `v`,
prove `Irrational ((u:ℝ) - (v:ℝ)/Real.pi^k)`. Specialize to `k=2*m`
for positive `m` and positive `v`. The required input is the actual,
unconditional kernel-checked external theorem `Transcendental ℚ Real.pi`,
not merely irrationality of π, and not an assumed transcendence axiom.

This proves the arithmetic inference from the displayed rational-minus-
correction form. Identification of the infinite trace series with that
form using Euler's formula, parity and the finite tails remains separate.
