# Statement lock: the literal determinant normalizer

The checked inherited-column order gives sigma=-1 in (16). Define
N_n(theta) = (-2*i) V(R)V(O) Q^(n+m+1) normSq(f), with all four factors
already defined from the actual characteristic roots and individual phases.
normSq(f) is exactly |f|^2, without a nonsmooth absolute-value derivative.

Prove N is smooth, nonzero for 0<theta<=pi, and conjugate(N)=-N. Together
with the proved conjugate(D)=-D this shows the actual quotient D/N is
real. Its zero condition is equivalent to the actual matrix eigenvalue
condition for 0<theta<pi. No identity with sin(F)+E or error bound is
assumed; those remain the next assembly step.
