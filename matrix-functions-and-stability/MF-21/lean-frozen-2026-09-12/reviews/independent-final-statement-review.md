# MF-21 independent final statement referee report

**Verdict: APPROVE — source fidelity and top-level premise discharge, within the scope below.** No material mathematical statement correction was found. This verdict does not certify a compiler, Comparator, kernel replay, or sandbox execution.

**Reviewer and independence.** Codex agent `/root/mf21_final_statement_referee`, 21 September 2026 UTC. This reviewer did not implement the MF21Restart proof or its numerical repair, did not edit any Lean source, and did not compile Lean. The review follows the source-fidelity lane of `reviews/REFEREE_STANDARDS.md`, with limited checks of numerical scope and the contract boundary. I did not read the earlier component review verdicts as a basis for approval. I independently read the frozen problem and manuscript, the target and wrappers, the assembly, and the selected dependencies recorded below. This is an AI-agent review, not external human peer review.

**Authoritative scope.** The mathematical comparison is against `original-proof/problem.md` and the frozen `original-proof/solution.md`, SHA256 `6e1bb10bb2ccfe1311775a6d24ca292113036867e080185741ef4f5d5b8174aa`. The manuscript's historical claims and status labels were not replaced by current upstream mathematics. I performed no upstream, fork, branch, PR, novelty, or current-status search. This review cannot support a new mathematical-resolution count or an additional distinct-problem count.

**Finding 1 — exact canonical quantifiers and ranges.** `MF21Restart/Definitions.lean:75` defines the complete original three-part target. The outer quantifier is every natural `m >= 3`, equivalent to the stated integer range. One existential real coefficient family is chosen before all expansion-order, matrix-size, and eigenvalue-index quantifiers. Each order `k <= 2*m` is continuous on the closed interval `[0, pi]`; `d 0` equals the actual symbol there. The natural-indexed infinite family is a harmless extension of the finite list `d_0,...,d_{2m}`, since the target never uses a larger index. `UniformBound` at line 63 has a strictly positive real constant, a natural threshold, and every `n >= N` and `1 <= j <= n`; the constant and threshold may depend on the fixed `m`, coefficient family, and order, but not on `n` or `j`. Orders `p <= 2*m-1` include zero and the endpoint `2*m-1`. The natural subtraction agrees with integer subtraction under `m >= 3`.

`BulkBound` at line 68 retains precisely `ceil((log(n+2))^2) <= j <= n`, with natural logarithm and error exponent `2*m+1`. Its additional explicit `1 <= j` is redundant: the independently inspected `bulk_cutoff_pos` in `StatementBridges.lean:67` proves it for every natural `n`. The natural ceiling agrees with the original ceiling because the argument is positive. The final conjunct is `not UniformBound m d (2*m)` for that same `d`, matching the original existential three-part assertion. It is not a claim about all possible unrelated coefficient families, and the original target does not require such an extra universal quantifier. Empty index ranges at `n = 0` introduce no asymptotic loophole; both small and largest valid indices are retained for every positive size.

**Finding 2 — matrix, Fourier representation, and eigenvalue ordering.** `symbol` at `Definitions.lean:18` is literally `(2*sin(theta/2))^(2*m)`, equal to the manuscript's `(2-2*cos(theta))^m`. `mesh`, `expansion`, and `remainder` at lines 55–60 retain `j*pi/(n+2)`, the sum from zero through `p`, division by `(n+2)^k`, and the sign `lambda - expansion`. There is no change to an `n`, `n+1`, or `n+2m` normalization in the target.

The real cosine Fourier representation is justified by the complete proof of `complex_fourier_eq_cosine` in `StatementBridges.lean:44`, including the signed integer frequency, negative complex exponential, interval `[-pi,pi]`, and factor `1/(2*pi)`. The odd sine integral vanishes; integrability is supplied before splitting the complex integral. Consequently the original complex Toeplitz matrix is the scalar extension of the real matrix in the target. Using zero-based `Fin n` row and column indices leaves the difference `j-k` unchanged. I inspected the real/complex characteristic-polynomial bridge in `RealComplexEigenvalue.lean` as a further check that the proof uses the actual matrix.

`orderedEigenvalueList` at line 38 sorts the full Hermitian eigenvalue multiset, preserving multiplicity. The pinned Mathlib spectrum declarations identify that multiset with the characteristic-polynomial roots; pinned `Multiset.sort` defaults to ascending `<=` and preserves the multiset. The accessor at line 46 uses position `j-1` under `1 <= j <= n`. Thus `j=1` is the smallest eigenvalue and `j=n` is the largest; neither is dropped or duplicated. The totalized zero value outside the valid range is not used by any target estimate.

**Finding 3 — the stronger smooth theorem uses the manuscript family.** `TargetProof.lean:48` states the same three conclusions together with `ContDiffAt Real infinity (d k) x` for every `k <= 2*m` and every `x` in the closed interval. The pinned Mathlib definition distinguishes smooth order `infinity` from analytic order `top = omega`; the theorem weakens the constructed analytic regularity to smoothness, not the other way around. At the endpoints it supplies ambient real smoothness, which is at least the required closed-interval regularity.

The proof obtains one `Y` from `manuscript_implicit_taylor_with_spectral_error` and selects exactly `implicitPhaseCoefficient m Y`. Its definition at `ImplicitTaylor.lean:18` is the `k`-fold real derivative in `h` of `symbol m (Y (x,h))` at zero, divided by `k!`, exactly manuscript equation (5). `PhaseZero.lean` sums the individual principal arguments for indices `1,...,m-1`, identifies the positive-angle formula, and proves the unwrapped endpoint `eta(0)=(m-1)*pi/2`. The inspected implicit-phase construction supplies `Y=x+h*eta(Y)`, displacement, uniqueness, and all-order Taylor data for one `Y`. The extension-independence theorem independently compares arbitrary smooth competing parametrizations and extends coefficient equality to both endpoints. Finally `target_proved` at `TargetProof.lean:82` takes the same family from the smooth theorem and derives continuity; it does not choose a second family for the canonical result.

**Finding 4 — all assembly premises are discharged, without assuming the target.** The public `target_proved` has type `Target` with no further premise, and the smooth theorem has no premise beyond the published `m >= 3`. The analytic/spectral data theorem takes only `m >= 2` and is called with that consequence of the target range. Its Taylor and vanishing data are supplied by the actual implicit-phase construction; its spectral error is for `eigenvalue m n j` and the same `Y`, including `j=n`.

`implicitPhase_expansion_bounds_of_data` preserves the supplied `Y`. Its remaining finite-prefix eigenvalue premise is explicitly discharged in `TargetProof.lean` by `eigenvalue_fixed_prefix_bound` from `CirculantBounds.lean:129`. The latter is proved from actual circulant interlacing and bounds for the same ordered Toeplitz eigenvalues, rather than postulated or taken as a new target assumption. The bulk conclusion and every lower global order therefore concern the exact same coefficient family. I reviewed the relevant interfaces and their use directly; I did not merely rely on names or prior reviews.

**Finding 5 — the critical obstruction compares the same trace and normalization.** In `implicitPhase_critical_bound_impossible` at `TargetProof.lean:22`, the critical uniform bound is introduced under negation and used to derive a contradiction. It is not an assumption of the final theorem. `ActualFixedIndex.lean:18` uses this hypothetical bound, the already supplied Taylor estimate, the IFT displacement, and the actual phase endpoint to give equation (30) for every original fixed `j >= 1`, including indices below the tail cutoff. Its limit is `pi^(2*m)*(j+(m-1)/2)^(2*m)` with the correct one-based shift.

`SpectralTracePassage.lean:90` is conditional on fixed-index limits and a tail bound, and both are supplied in the top-level proof. The actual circulant theorem at `CirculantBounds.lean:202` gives the reciprocal majorant on `j >= 2`; the passage handles `j=1` by its own positive limit. Its zero-based summation index represents original index `k+1`, with all `n` eigenvalues included before extension by zero. Dominated convergence yields the series in manuscript equation (31) as a limit of `(1/(n+2))^(2*m) * trace((toeplitz m n)^(-1))`.

Independently, `ActualTraceLimit.lean:64` proves a limit for exactly that sequence using the actual inverse-grid limit and the explicit reflected kernel. The inspected kernel definitions reproduce equation (27), the diagonal and integral reproduce equations (28)–(29), and the constant is `((2*m-1)!)^2 / ((4*m-1)!*(2*m-1)*((m-1)!)^2)`. The `n`-to-`n+2` scaling is proved explicitly. `kernelTraceConstant_rational` supplies rationality, while `trace_series_irrational` supplies irrationality of the exact series, with the odd/even shifts intact for `m >= 3`. Uniqueness of limits gives the contradiction. No desired trace limit, inverse-kernel result, spectral majorant, or irrationality assertion appears as an unproved hypothesis of the final theorem. I did not re-audit the full lower-level inverse or transcendence proofs in this bounded statement review.

**Finding 6 — the seven exposed contracts match.** I read all seven declarations in both wrappers and performed a Python text comparison of their theorem headers, normalizing whitespace only: seven headers in each file, all equal. This is a source-text check, not elaborated type comparison. `Solution.lean` imports the project root and explicitly applies the correspondingly named proved theorems; `Challenge.lean` imports only the shared definitions and Mathlib calculus definitions. No proof import of `Challenge` was found. `comparator.json` lists all seven names, including both complete targets, with only `propext`, `Classical.choice`, and `Quot.sound` permitted. The shared definitions remain at the locked hash. Seven deliberate challenge placeholders are separate from the proof and are not definition holes. These two complete-target contracts concern one original MF-21 problem.

A source keyword scan of `MF21Restart/`, `MF21Restart.lean`, and `Solution.lean` found no `sorry`, `admit`, custom `axiom` declaration, `unsafe`, or `native_decide` proof shortcut. The match for “axiom” in `PiTranscendence.lean` was a comment; that wrapper imports an actual named vendored theorem. This scan does not replace an import-closure axiom audit or kernel replay.

**Finding 7 — the numerical repair preserves its sole target.** I inspected the prior numerical implementation and the new minimal-certificate source separately. Both export exactly `phase_window_margin : (1/4 : Real) < sin(pi/4)`. In the candidate, the only real residual is `1 <= sqrt(2)`. The pinned LeanCert definition at scale zero computes the rational lower enclosure from `Nat.sqrt 2`, so its value is one. The source uses `leancert_verify_cert` with kernel trust to prove that closed rational equality, feeds it essentially into `sqrtRatLowerPrec_le_sqrt`, then applies the exact sine identity and linear arithmetic. It includes both `#assert_trust kernel phase_window_margin` and `#print axioms phase_window_margin`. There is no parameter search, subdivision, eigenvalue approximation, or numerical integration. This is an appropriate minimal computational certificate for the selected residual. I did not compile this candidate or measure its memory use.

**Execution evidence and metadata boundary.** `formalization.yaml` correctly identifies the frozen problem/manuscript and the complete same-family target, retains attribution and George Stepaniants's Caltech affiliation without an email, and distinguishes local checking from Linux Comparator evidence. The local historical record `evidence/runs/20260921T000301098707Z/record.json` has SHA256 `abdf520ffbcfc2fe80236e0c303795d97a50e8c11f46a5edbac8a29dba5d7856`; I inspected its metadata, not re-executed it. It records 125 runs, 386 distinct axiom entries with only the three standard axioms, `passed: true`, one thread, 4096 MiB, and `comparator: not_run`. Its Numerics hash is the prior `9d8eb86229773759393cecd14b2651dfc6a994c270dc1ee82d3131c1af60bc42`, not the repaired candidate hash below. Historical success therefore does not establish source-matched success of the repair.

The coordinator reports that GitHub run `35548010081` failed and that a new local serial integration run is active. I have not independently fetched that GitHub run, inspected a successful Linux run, invoked Lean/Comparator, or validated every historical log/output hash. No Linux Comparator, kernel replay, or sandbox success is asserted here. Final verification prose must attach the repaired source hashes to their own successful local evidence and report the actual final GitHub result. That is an execution/publication follow-up, not a mathematical statement change requested by this referee.

**Review limits.** This is a final statement and assembly review with selected dependency inspection, not a fresh review of every proof line in the 125-source project. The deep determinant, root counting, interlacing, inverse-kernel derivation, zeta/transcendence proofs, and build/sandbox machinery retain their separately documented proof-review and execution obligations. No defect found in this narrower scope should be represented as an independent all-project compiler or proof audit.

**Exact source scope and hashes.** Paths below are relative to the reviewed local project. I compared the fully read primary files and selected dependency sources against the publication worktree: all agreed except Numerics, where that worktree still held the historical implementation at the comparison time, and the new numerical statement lock, which had not yet been copied. The report covers the repaired local Numerics at its exact hash, as well as the unchanged exported statement. It is written to the staging review directory at the coordinator's request. Later file changes require checking the applicable scope again.

| Source | Read scope | SHA256 |
| --- | --- | --- |
| `original-proof/problem.md` | complete file | `1aac38ed3c7a83dfc3dddb4beeec26fcf117f23f5789b2a374c1332ba4cf359f` |
| `original-proof/solution.md` | complete file | `6e1bb10bb2ccfe1311775a6d24ca292113036867e080185741ef4f5d5b8174aa` |
| `MF21Restart/Definitions.lean` | complete file | `35a74047a81a9b7526e9bf039880ca8f07ae33314e2b5b960ab254e02fca4b51` |
| `MF21Restart/TargetProof.lean` | complete file | `189eb2e1ed8ad943d78c982e36203cae995e0c6b731293161cb615c9228edf53` |
| `Challenge.lean` | complete file | `9bb51fae68bab7a4ba5ff8fe7c3b815e361f31c041897ee04596dfd0215989b2` |
| `Solution.lean` | complete file | `97dffa0a35b7067b784763adcdf01c21f8da7d005a69c32662dbe9c84e080659` |
| `comparator.json` | complete file | `dabddad03ffd9e58d22f1712583ee686d166ede59ebe073798fddd57a928c3ab` |
| `COMPARATOR_TARGET_STATEMENTS.md` | complete file | `a1c87983730695ca19aa94e328909a3f94ac43a8ce3f9630b0f1cecd42f05774` |
| `NUMERICS_MINIMAL_CERTIFICATE_STATEMENTS.md` | complete file | `ac95aa3b3aee13f8c14ecac1cb15914d98efc0c5c2a91c0babafe2fc7e71b0f9` |
| `formalization.yaml` | complete file | `3448a61dc1a77ea9e13dd672ad4be93544f0aabf5f4123850b7896ec1368e3a3` |
| `reviews/REFEREE_STANDARDS.md` | complete file | `e4771ab0a1c2f51541a2b849ec82fadaf7fc927ba4984295d25b08995d1947f1` |
| `MF21Restart/Numerics.lean` | complete file | `c4677460fab467e2992c9fd0b1d74abf0d682ba8a7c89857d77e2b481a89e410` |
| `MF21Restart/StatementBridges.lean` | complete file | `90992b2cf34bbbefdf9da9c04b814a693faba7ec8d48b16bdf8d00948c4db057` |
| `MF21Restart/ImplicitSpectralData.lean` | complete file | `e379ad2cfd444ba82aa0f10e9c4d51c2bf61d081836754c24ae7c2029fd1bc1b` |
| `MF21Restart/ImplicitExpansionBounds.lean` | complete file | `53ac303b8ee106c09898d46716c75ee28e0b0207342996dd68d3dcc3255bf955` |
| `MF21Restart/ActualFixedIndex.lean` | complete file | `675e4abb114e5ebe8f824d7749096f520b315542303e634db1eb452c99a0b0be` |
| `MF21Restart/ActualTraceLimit.lean` | complete file | `192550076632c3ac021957564d7717b5300e0451de69301aee308ab58dd7473e` |
| `MF21Restart/ImplicitTaylor.lean` | complete file | `f91067cf235fc4afd4b869a8b605a15a46a9c0405484550ad10db351d0d2296d` |
| `MF21Restart/PhaseZero.lean` | complete file | `ac803ba9621d41d11d7ccff77c53299fdf6f13f9caf98e99b6692d4a3cb78885` |
| `MF21Restart/SpectralTracePassage.lean` | complete file | `4366c06110bbd59395afb9993704f8dac810bf573921a37aa8624e79ebff5610` |
| `MF21Restart/TraceSeries.lean` | complete file | `7c1b2aaf0fddd59cacf63e058cc226917dd6a12207402e6e6373e1bed4ba8a59` |
| `MF21Restart/ActualKernelDiagonal.lean` | complete file | `6deda38f66840d6e29a073a890ad288b931d346e483b44aee2f90963b6e3c81c` |
| `MF21Restart/CirculantBounds.lean` | complete file | `f63c776c46312d11347199ae3eb42003725dd95c578012b72f490fbb9a2a86ff` |
| `MF21Restart/ExtensionIndependence.lean` | complete file | `bd4acdd599b5a263020c292231426399292f7e13a83cca076d88c9b01cc8f126` |
| `MF21Restart/InverseKernelGrid.lean` | complete file | `48629ecc6165f1343374ad5f20b3805cdd88c2f17441039fba2308bf2864afc9` |
| `MF21Restart/InverseKernelLimit.lean` | complete file | `e7a8f48440570bb2ca8d62c770d252618e3d4eeaecc1daceed254255282e5ea9` |
| `MF21Restart/ImplicitPhase.lean` | complete file | `db3e9ecbaa3dac159bc6bd736363794cbeb09afeaa1fd815a8b3d959b093a1dc` |
| `MF21Restart/ImplicitSpectralError.lean` | complete file | `41da02350921c1a6e8cfae0c366ab1801e549c10d554752083f567705566cf2e` |
| `MF21Restart/RealComplexEigenvalue.lean` | complete file | `86f2f2cb01dea730e5c6c2042fa52652c25f0d99a683c6ffaef12bb404c8f2a0` |
| `MF21Restart/PiTranscendence.lean` | complete file | `63978695081b2c4e863609090b0ae358470ae7e64e17893385237339d8315cc9` |
| `TARGET_ASSEMBLY_STATEMENTS.md` | complete file | `466dc09381e2b2fdcec3cae8883be07b1c09de815d4be4076b18d07313c0df80` |
| `lake-manifest.json` | complete file | `408ea491dc48a404e8703c56403d3d252fa7bdc4feb393951e34339675a5e2bc` |
| `original-proof/problem-provenance.json` | complete file | `fc94294687575bde1a2c4adbd1e0b4cf6e19a6707e4abb676e058d7261bf8960` |
| `VERIFICATION.md` | complete file | `3695d1339977f1c417f27e0b67cdfce4c7c90f9abbf5ab01bca4e91a0c68bafc` |
| `MF21Restart/InverseKernelGridTop.lean` | lines 1–115 | `b323ba3ed3af5e51044ca6af6b8a96cef70481cd70bf3dc3c22c438563de1b2a` |
| `MF21Restart/InverseKernelContinuity.lean` | lines 1–125 | `63299f9afc95a00d783086a93cf6dc9205790d7fefbc6b4312b02526a05f7e4d` |
| `MF21Restart/ScaledRising.lean` | lines 34–72 | `23575eba296f34e65d9d5fc952bd707f64415206ad79488a06772e3c1313ac4f` |
| `MF21Restart.lean` | import declarations | `6e12186336a3799391b8b6b1cb872250ef1f4e19d2062c32d25258ad59e23a7d` |
| `.lake/packages/mathlib/Mathlib/Analysis/Matrix/Spectrum.lean` | lines 47–84 and 147–180; selected declaration search | `1ad47effa0a5fbc373ebb6cf52a6b85a66b25d3c5a86394f3dd5932ffbfc3afe` |
| `.lake/packages/mathlib/Mathlib/Data/Multiset/Sort.lean` | lines 23–57 | `da36e6ab5437d2691f19cd8721c37d3f263c01f38385d26f1db3ed61628d2329` |
| `.lake/packages/mathlib/Mathlib/Analysis/Calculus/ContDiff/Defs.lean` | selected definition/documentation search for infinity, omega, ContDiffAt | `5d2f01069c6265873a372d4aed746c5d3411073b8acb108a180d02a84c832dca` |
| `.lake/packages/leancert/LeanCert/Core/IntervalRat/Transcendental.lean` | lines 211–236 and 395–406 | `3968b194323b625283fae2c53900340ff13ec6eaf7d52e958e5c83246f580272` |

The inspected local dependency checkout heads match `lake-manifest.json`: Mathlib `0df444a360eaa60ab8c11dca51a86af692955474`; LeanCert `621a43d7cf21f87872392a01e874f2f1dbddc926`. Dependency-file hashes above identify the read excerpts' containing files; they do not imply that the full files were audited.

Read-only checks used `cat`/`sed`/`rg`, Python `hashlib` and JSON inspection, a whitespace-normalized header comparison, byte comparisons with the publication worktree, and `git rev-parse HEAD` for dependency pins. No shell command in this review invoked Lean, Lake compilation, or Comparator. The sole write was this referee report.

Report generated UTC: 2026-09-21T01:07:46.897297+00:00
