**Independent final MF-21 proof-assembly and trust review**

Reviewer: `mf21_final_proof_referee`. Review date: 21 September 2026 UTC. **Verdict: APPROVE for the source-level proof assembly and the repaired numerical certificate described below.** No material mathematical or proof-trust defect was found in this scope. This is not a successful fresh-build, Comparator, kernel-replay, or sandbox verdict.

I had no authorship role in this development. I did not modify Lean sources, invoke Lean or Lake, push, or start GitHub Actions. I read the actual staging sources, followed the main assembly's helper signatures and their supplying proof paths, checked the local import graph and contracts, and independently inspected the pinned LeanCert certificate implementation and soundness proof. This report follows `reviews/REFEREE_STANDARDS.md`, SHA256 `e4771ab0a1c2f51541a2b849ec82fadaf7fc927ba4984295d25b08995d1947f1`, the project's pinned Tau Ceti adaptation.

This is a bounded assembly/trust review, not a second detailed mathematical audit of every line in all 125 modules. I fully read the main files named below, and inspected selected dependency statements and proof paths where they discharge an assembly hypothesis. Earlier determinant, root-counting, inverse-factorization, Riemann-sum, and vendored transcendence developments retain their separate component-review scopes. Complete manuscript-to-statement correspondence is assigned to a separate final referee. The only manuscript mathematics I consulted was the unchanged `original-proof/solution.md`, especially (21)–(33); I did not use the current upstream MF-21 proof or its separate formalization.

The reviewed source tree is `/Users/georgestepaniants/Research/OpenProblemsInNLA/lean-verification/MF21-restart`. Its 125 Lean files have aggregate SHA256 `0dc27e2cca2a98b89580bddfc6f6ad8e3a06e0524c690a4886e9c89030bdc023`: this hashes the UTF-8 concatenation of `<sha256>  <relative-path>\n`, ordered lexicographically by the POSIX relative-path string, over root `.lean` files and every `.lean` file under `MF21Restart/` and `LeanFormalizations/`. It excludes dependencies, evidence, and review prose. Individual hashes for the principal reviewed inputs follow.

| Input | SHA256 |
|---|---|
| `original-proof/solution.md` | `6e1bb10bb2ccfe1311775a6d24ca292113036867e080185741ef4f5d5b8174aa` |
| `original-proof/problem.md` | `1aac38ed3c7a83dfc3dddb4beeec26fcf117f23f5789b2a374c1332ba4cf359f` |
| `MF21Restart/Definitions.lean` | `35a74047a81a9b7526e9bf039880ca8f07ae33314e2b5b960ab254e02fca4b51` |
| `MF21Restart/TargetProof.lean` | `189eb2e1ed8ad943d78c982e36203cae995e0c6b731293161cb615c9228edf53` |
| `MF21Restart/ImplicitTaylor.lean` | `f91067cf235fc4afd4b869a8b605a15a46a9c0405484550ad10db351d0d2296d` |
| `MF21Restart/CoefficientVanishing.lean` | `2444222b2fd0560e420a477ccfad6bc87cc7a7f3a9c4a24ce3855f27a82327ff` |
| `MF21Restart/ImplicitSpectralData.lean` | `e379ad2cfd444ba82aa0f10e9c4d51c2bf61d081836754c24ae7c2029fd1bc1b` |
| `MF21Restart/ImplicitSpectralError.lean` | `41da02350921c1a6e8cfae0c366ab1801e549c10d554752083f567705566cf2e` |
| `MF21Restart/ImplicitExpansionBounds.lean` | `53ac303b8ee106c09898d46716c75ee28e0b0207342996dd68d3dcc3255bf955` |
| `MF21Restart/ExpansionAssembly.lean` | `3a9fc651f39e373f88780653b6f95946292d667c3ae791ba861e337d471d431d` |
| `MF21Restart/ActualFixedIndex.lean` | `675e4abb114e5ebe8f824d7749096f520b315542303e634db1eb452c99a0b0be` |
| `MF21Restart/FixedIndex.lean` | `da98fb1baae1b4b1736f5be8f03261b4a609182342df6158ac2394713313fc28` |
| `MF21Restart/CirculantBounds.lean` | `f63c776c46312d11347199ae3eb42003725dd95c578012b72f490fbb9a2a86ff` |
| `MF21Restart/SpectralTracePassage.lean` | `4366c06110bbd59395afb9993704f8dac810bf573921a37aa8624e79ebff5610` |
| `MF21Restart/InverseSpectralTrace.lean` | `4cd1c1189a56438237db1f217bc6c3172032f955ff5d030f86cd7f4fd1129b1d` |
| `MF21Restart/ActualTraceLimit.lean` | `192550076632c3ac021957564d7717b5300e0451de69301aee308ab58dd7473e` |
| `MF21Restart/InverseKernelGrid.lean` | `48629ecc6165f1343374ad5f20b3805cdd88c2f17441039fba2308bf2864afc9` |
| `MF21Restart/ActualKernelDiagonal.lean` | `6deda38f66840d6e29a073a890ad288b931d346e483b44aee2f90963b6e3c81c` |
| `MF21Restart/TraceSeries.lean` | `7c1b2aaf0fddd59cacf63e058cc226917dd6a12207402e6e6373e1bed4ba8a59` |
| `MF21Restart/TraceIrrationality.lean` | `886aa32e8f60a28e4ec339d616d17e67e6f1241b99ba51020ad1e48815cd73d9` |
| `MF21Restart/PiTranscendence.lean` | `63978695081b2c4e863609090b0ae358470ae7e64e17893385237339d8315cc9` |
| `MF21Restart/Numerics.lean` | `c4677460fab467e2992c9fd0b1d74abf0d682ba8a7c89857d77e2b481a89e410` |
| `MF21Restart.lean` | `6e12186336a3799391b8b6b1cb872250ef1f4e19d2062c32d25258ad59e23a7d` |
| `Challenge.lean` | `9bb51fae68bab7a4ba5ff8fe7c3b815e361f31c041897ee04596dfd0215989b2` |
| `Solution.lean` | `97dffa0a35b7067b784763adcdf01c21f8da7d005a69c32662dbe9c84e080659` |
| `comparator.json` | `dabddad03ffd9e58d22f1712583ee686d166ede59ebe073798fddd57a928c3ab` |
| `lakefile.toml` | `3237f2e50720c93b153a5179b89ebd9e30f84687193079c9d3628dee99931d31` |
| `lake-manifest.json` | `408ea491dc48a404e8703c56403d3d252fa7bdc4feb393951e34339675a5e2bc` |
| `lean-toolchain` | `3aac669c7a910ec2389f4e4f921b605adf6ebf2d1e0c9b9cd0be4d33f3f5db71` |

The assembly has the following concrete discharge paths.

1. **Actual matrices and index range.** `Definitions.lean` defines `fourierCoeff` by its real integral and `toeplitz` by those coefficients; it sorts the Hermitian spectrum, then converts the published one-based index to `j - 1`. The target only uses `1 ≤ j ≤ n`. `StatementBridges.complex_fourier_eq_cosine` proves the original negative-exponential complex Fourier formula with its normalization, so the real integral is connected to the source matrix. The inspected `SpectralEnclosure.toeplitz_posDef` derives positivity from the actual weighted Fourier quadratic integral. Thus positivity and reciprocal operations in the inspected trace path concern that matrix, not a freely supplied eigenvalue sequence.

2. **One constructed phase and coefficient family.** `manuscript_uniform_implicit_taylor` constructs `Y` from `manuscriptEta_uniform_implicit_phase`, then defines every coefficient by `iteratedDeriv k (fun h => symbol m (Y (x,h))) 0 / k!`. `manuscript_uniform_implicit_taylor_with_vanishing` retains that same `Y` and adds the proved coefficient bounds. `manuscript_implicit_taylor_with_spectral_error` retains it again. Its tail estimate uses `eigenvalue_implicit_phase_error_of_unique`: the actual phase preimage is identified with `Y` by interval uniqueness, then the symbol derivative estimate converts the phase error to the eigenvalue error. The supplying `eigenvalue_eventual_quantitative_phase_preimage` has only `2 ≤ m` as a hypothesis and explicitly identifies `symbol m θ = eigenvalue m n j`; the phase-window indexing theorem also includes `j = n`.

3. **Global and bulk estimates.** `TargetProof.manuscript_smooth_target` obtains the above data once. It passes the same `Y`, Taylor bound, vanishing bound, and actual tail bound to `implicitPhase_expansion_bounds_of_data`. That theorem's conditional global conclusion is then applied to `eigenvalue_fixed_prefix_bound m hm1`. I read `CirculantBounds`: this supplying theorem follows from the actual principal-block circulant interlacing and sine estimates. It has no expansion hypothesis. In `ExpansionAssembly`, the cases `j < J` and `J ≤ j` cover every original index, and the power comparison is justified by `p ≤ 2*m - 1` and `0 < 1/(n+2) ≤ 1`. The bulk proof uses `eventually_bulk_cutoff_ge` and the logarithmic-squared scale-gain theorem to extract the extra power of `1/(n+2)` from the exponential tail. No alternate coefficient family is chosen for either conclusion.

4. **Critical assumption and every fixed index.** `implicitPhase_critical_bound_impossible` introduces only the negated critical `UniformBound` as its contradiction hypothesis. `critical_bound_implies_actual_fixed_index_limit` obtains smallness of `Y(mesh n j,1/(n+2))` from the uniform displacement bound for each fixed `j ≥ 1`, including `j < J`. It supplies the actual implicit equation, the critical Taylor remainder, continuity of `manuscriptEta` at zero, and its proved endpoint value. `FixedIndex.critical_bound_implies_fixed_index_limit` cancels the same complete critical expansion, including coefficient `2*m`, between the hypothetical uniform bound and Taylor estimate. The resulting fixed-index spectral limit is therefore conditional on the contradiction hypothesis; no false unconditional assertion about the lowest eigenvalues is imported.

5. **Dominated trace passage.** `InverseSpectralTrace.toeplitz_inverse_trace_one_based` follows from positive definiteness, spectral diagonalization, and a permutation of the sorted spectrum. `inverseSpectralTerm m n k` is the scaled reciprocal at original index `k+1` when `k<n`, and zero otherwise. Its sum includes both `j=1` and `j=n`. `SpectralTracePassage` proves positivity, termwise reciprocal convergence, and the trace identity before applying dominated convergence. The explicit `CirculantBounds.eigenvalue_eventual_reciprocal_tail_bound` applies to `j ≥ 2`; the proof bounds `k=0` separately by its own strictly positive fixed-index limit. The majorant is a summable `2*m` power tail plus a single finite exceptional term. There is no unproved interchange of limits and infinite sums.

6. **Independent rational trace limit and contradiction.** `ActualTraceLimit.toeplitz_inverse_trace_tendsto_n` uses actual uniform inverse-grid convergence and the continuous diagonal Riemann average, then `toeplitz_inverse_trace_tendsto_mesh` converts `n` scaling to `n+2` scaling. I followed the input through `InverseKernelGrid` to the supplying `InverseKernelGridTop` statements and actual inverse-entry formula in `ToeplitzInverse`; they identify `(toeplitz m n)⁻¹` explicitly and do not assume a trace limit. The reversal-grid shift `1/n` is retained in `InverseKernelGrid`. `ActualKernelDiagonal` proves the diagonal identity, its integral, and rationality of the factorial expression. `TraceSeries` proves irrationality of the very same normalized series, using the integer/half-integer tails and the positive rational correction. `3 ≤ m` gives the required nonzero correction; the transcendence step uses the proved `PiTranscendence.pi_transcendental` wrapper over the vendored theorem, not a literature axiom. `TargetProof` applies uniqueness of limits and contradicts that irrationality with `kernelTraceConstant_rational`.

All remaining top-level premises are arithmetic consequences of `3 ≤ m` or are components of the single constructed data tuple. `manuscript_smooth_target` retains the same family for smoothness, the leading coefficient, all subcritical orders, the bulk estimate, and failure of the critical uniform estimate. `target_proved` merely weakens its smoothness to the canonical continuity requirement. I found no undischarged conditional helper, circular expansion assumption, or second existential coefficient choice at this assembly boundary.

The local import walk contains **123 modules in the Solution closure**, including all six vendored transcendence modules and excluding `Challenge` and `Audit`. The local part of the Challenge closure contains only `Challenge` and `MF21Restart.Definitions`. After stripping nested block comments, line comments, and strings, a lexical scan of the Solution closure found no `sorry`, `admit`, `axiom`, `unsafe`, `native_decide`, `implemented_by`, `extern`, `ofReduceBool`, `ofReduceNat`, `trustCompiler`, `run_tac`, `run_elab`, or `run_cmd` token. The seven intentional `sorry` occurrences are all in Challenge. This is a source/import check; it is not substituted for a transitive kernel axiom audit of external dependencies.

I compared all seven Challenge and Solution theorem signatures after whitespace normalization and checked their fully qualified names against `comparator.json`; both comparisons match exactly. The configuration has no definition-hole entry and permits only `propext`, `Quot.sound`, and `Classical.choice`. Its full-target and same-family smoothness contracts are included. The source definitions do not assume Target. The repaired Lakefile lists exactly the six existing vendored `.lean` modules as roots; every library retains `-j1 -M4096`. This resolves the observed source-level registration omission, but only a fresh build can establish execution readiness.

The new `Numerics.lean` proves the unchanged `phase_window_margin`. It rewrites `sin(pi/4)` to `sqrt(2)/2`, obtains the closed certificate `sqrtRatLowerPrec (2 : ℚ) 0 = 1`, applies `sqrtRatLowerPrec_le_sqrt` at `q=2` with its nonnegativity hypothesis proved by `norm_num`, and finishes by linear arithmetic. It certifies a lower bound; it does not equate `sqrt 2` with 1. Scale zero makes the computation `Nat.sqrt 2`, with denominator and scaling factor both one. There is no interval subdivision, adaptive precision search, matrix enumeration, numerical integration, or externally trusted approximation. The one margin theorem is reused by the phase-window and coverage proofs.

I independently checked `.lake/packages/leancert` at clean Git HEAD `621a43d7cf21f87872392a01e874f2f1dbddc926`, equal to the manifest pin. The inspected `LeanCert/Core/IntervalRat/Transcendental.lean` has SHA256 `3968b194323b625283fae2c53900340ff13ec6eaf7d52e958e5c83246f580272`; `LeanCert/Tactic/Verification.lean` has SHA256 `2c576708b528acdde17796b0b715b83079f9cad377182dbd87c248323ff0a58c`.

In `Transcendental.lean`, lines 173–178 identify `intSqrtNat` with `Nat.sqrt` and its proved square bound; lines 220–230 define the precise lower bound. I read the complete soundness proof at lines 400–468: it derives the real inequality from that integer square-root bound, positive denominator and powers, rational casts, and real square-root algebra. It is a theorem proof, not an axiom. In `Verification.lean`, lines 265–294 require a closed decidable proposition; lines 328–347 construct a decision proof and register it through `mkAuxLemma` with asynchronous elaboration disabled. The `.kernel` branch at lines 501–511 has no native fallback. `Numerics.lean` explicitly sets `leancert.trust` to `"kernel"`, and `leancert_verify_cert` reads that configuration. Its use of this low-level tactic is tied to this pinned implementation. The final `#assert_trust kernel phase_window_margin` collects transitive axioms and rejects native-compiler, sorry, and custom trust. The verifier module contains separate native machinery, but the selected proof path does not use it.

Execution evidence was inspected read-only and must be distinguished as follows.

- The historical complete local record `evidence/runs/20260921T000301098707Z/record.json`, SHA256 `abdf520ffbcfc2fe80236e0c303795d97a50e8c11f46a5edbac8a29dba5d7856`, reports 125 exit-zero direct local Lean invocations with one thread and 4096 MiB. I recomputed every retained log hash and independently parsed all 386 distinct axiom reports in `Audit.log`, SHA256 `a1e73e7919dc4a8877bf5e2814c9b225c8781739818459a6170f50d7d646d098`; they match both `Audit.lean`'s declaration set and the record, and use only the three permitted axioms. Exactly one current Lean source differs from that historical source map: Numerics. Its old hash was `9d8eb86229773759393cecd14b2651dfc6a994c270dc1ee82d3131c1af60bc42`. The historical Lakefile also predates the six-root repair. Consequently that successful run is not a complete run of the current repaired package.
- The actual local candidate test `evidence/logs/numerics-minimal-candidate-02.json`, SHA256 `80cf97c5c8d989c454d98f2970e0c7a77fa0f2ecccf9e5d7dc58c4d77e38f987`, records the current Numerics bytes, an exit-zero command with `lake env lean -j1 -M4096 -R /private/tmp /private/tmp/MF21NumericsCandidate.lean -o /private/tmp/mf21-numerics-candidate-test/Numerics.olean -i /private/tmp/mf21-numerics-candidate-test/Numerics.ilean -c /private/tmp/mf21-numerics-candidate-test/Numerics.c --json`, and `LEAN_NUM_THREADS=1`. It ran from `2026-09-21T00:55:49.026591+00:00` to `00:56:01.474481+00:00`. I matched the source and log hashes; the log SHA256 is `cf12e33044415f5b0f465b37eda09124ec5693822d32ad0bb328bce7241777be`, and its margin-theorem axiom report contains only the permitted three. This is local Lean evidence, not Comparator or the Linux Lake `--setup` invocation.
- The coordinator's replacement full serial run under `evidence/runs/20260921T005811719722Z/` was still active during this review; no completed `record.json` existed when I inspected it. I do not count this pending run as successful. A subsequent source-matched record is required before asserting a complete local rebuild of the repaired package.
- The retained receipt for [GitHub run 35548010081](https://github.com/sgstepaniants/OpenProblemsInNLA/actions/runs/35548010081) identifies commit `e6f038f48b3039df030237424c05be745e33ce24`, artifact `10617142788`, and conclusion **failure**. I read that external receipt, SHA256 `2d426775791f384f530f4239d1c4fc2e9d05f07658ad8eb6d920021d211d5031`, and the actual project `comparator.log`, SHA256 `f890c287edc9f99a7c7cc777e15b1c92d3c7a1ee51fd56919d90a501ca6918b9`, in publication `references/mf21-frozen-lean-2026-09-21/`. The log reports the missing `MonicRootSums.olean` at line 172 and Numerics' interpreter memory exception at line 1010, followed by build failure. Successful verifier fixtures in other logs cannot certify this failed MF-21 build. No final MF-21 Comparator or kernel-replay acceptance was available to this reviewer.

One documentation discrepancy was reported to the coordinator and resolved before this report was finalized: `NUMERICAL_TARGETS.md` initially described `interval_decide (trust := kernel)`. I reread the corrected file, SHA256 `aa2e9278007b5f4f9d2e1a6d443d046e8f34d264a4e244eef46648eaf5272d05`, at `2026-09-21T01:10Z`; it now accurately describes the scale-zero rational certificate and `leancert_verify_cert`/soundness-lemma path. This required no mathematical or Lean-source change. Current-source local execution and eventual Linux results still require their own accurately dated records; they must not be inferred from this source-scope approval.

This report adds no new mathematical resolution or completed-verification count. It approves the identified proof assembly and numerical trust design, while leaving final runtime acceptance and the separate full statement-correspondence review to their actual evidence.
