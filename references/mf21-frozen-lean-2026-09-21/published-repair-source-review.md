**MF-21 published repair: independent source binding review**

**Verdict: APPROVE for the published source/evidence binding and unchanged workflow scope.** No mismatch was found. The subsequent Linux project build failed; this report gives no target Comparator, kernel-replay, or sandbox acceptance verdict.

Reviewer: Codex agent `mf21_final_proof_referee`, 21 September 2026 UTC. I did not author the proof, repair, runner, or compiler records. I previously reviewed the selected proof-assembly and numerical-trust scope. This follow-up compares immutable Git blobs with retained local execution evidence; it does not expand that mathematical review. I ran only read-only Git/Python inspections and wrote this external report. I ran no Lean compiler, Lake build, push, workflow dispatch, or GitHub status query.

The inspected commit is **`bd3014e699f277500beabda57cfeb1f57925ab78`**, and the exact project is `matrix-functions-and-stability/MF-21/lean-frozen-2026-09-12`. I used `git ls-tree`, `git show`, and `git cat-file --batch` at that commit, rather than substituting the worktree's later HEAD or mutable files. The source comparison was performed at `2026-09-21T01:30:53Z`, with contract and workflow checks following it. The applicable review rubric remains the pinned Tau Ceti adaptation in the project.

The committed completed local record is `evidence/runs/20260921T005811719722Z/record.json`, SHA256 **`497c98bffe6628a5adef5653adfb73cf25e04fd27f7306176aea9e34260842c5`**. It records coordinator execution from `2026-09-21T00:58:11.719722+00:00` through `2026-09-21T01:13:32.503384+00:00`. My independent comparisons found:

- All **125 committed Lean source hashes** match the record, and the committed `.lean` inventory equals its exact source set. The source aggregate is `0dc27e2cca2a98b89580bddfc6f6ad8e3a06e0524c690a4886e9c89030bdc023`, computed by sorting POSIX relative-path strings, concatenating UTF-8 `<SHA256><two spaces><relative path><LF>` lines, and hashing the result.
- All **three pin files**, the current and archived runner, the archived Lakefile, and the frozen manuscript match the record. `evidence/latest-local.json` is byte-identical to it. All ten dependency revisions in the committed manifest equal the record's package HEAD map. This is a committed-pin comparison, not a new inspection of external dependency checkouts.
- All **125 retained log hashes** match. Each recorded command is exactly `lake env lean -j1 -M4096 -o .lake/build/lib/lean/<source without .lean>.olean <source>`, with `LEAN_NUM_THREADS=1` and exit zero. I parsed `verify_local.py` with `ast.literal_eval`, without executing it: its 125 unique source paths exactly match both the source inventory and recorded invocation order. The inspected runner uses blocking calls under the shared compiler lock.
- All **386 actual axiom reports** parsed from the committed `Audit.log` match the committed `Audit.lean` declaration set and the record's axiom map. Only `propext`, `Classical.choice`, and `Quot.sound` occur. The Audit log hash is `a1e73e7919dc4a8877bf5e2814c9b225c8781739818459a6170f50d7d646d098`.
- Every project tree entry is a regular file. No `.lake` content, compiled `.olean`/`.ilean`, or `.o`/`.a`/`.so`/`.dylib` artifact is committed. This publication audit therefore does not recompute compiled-output hashes; those remain recorded local execution evidence with the separate evidence referee's stated inspection scope.

The seven committed Challenge and Solution signatures match after whitespace normalization, and their fully qualified names equal `comparator.json` exactly. The config permits only the three standard axioms and contains no definition-hole field. Both complete-target contracts are present. All 29 individual source/configuration hashes in my committed proof-assembly report match the actual published blobs; that report's SHA256 is `dd3314cd16f23b78a26003d7da0b4d1e849f72137cf8d3d4616d2edf92f4a3db`. Thus the previously reviewed source scope binds to this publication. Relative to the failed initial publication `e6f038f48b3039df030237424c05be745e33ce24`, the only changed Lean source is the already reviewed `MF21Restart/Numerics.lean`.

| Committed input | SHA256 |
|---|---|
| `MF21Restart/Definitions.lean` | `35a74047a81a9b7526e9bf039880ca8f07ae33314e2b5b960ab254e02fca4b51` |
| `MF21Restart/TargetProof.lean` | `189eb2e1ed8ad943d78c982e36203cae995e0c6b731293161cb615c9228edf53` |
| Repaired `MF21Restart/Numerics.lean` | `c4677460fab467e2992c9fd0b1d74abf0d682ba8a7c89857d77e2b481a89e410` |
| `Challenge.lean` | `9bb51fae68bab7a4ba5ff8fe7c3b815e361f31c041897ee04596dfd0215989b2` |
| `Solution.lean` | `97dffa0a35b7067b784763adcdf01c21f8da7d005a69c32662dbe9c84e080659` |
| `comparator.json` | `dabddad03ffd9e58d22f1712583ee686d166ede59ebe073798fddd57a928c3ab` |
| `lean-toolchain` | `3aac669c7a910ec2389f4e4f921b605adf6ebf2d1e0c9b9cd0be4d33f3f5db71` |
| Repaired `lakefile.toml` | `3237f2e50720c93b153a5179b89ebd9e30f84687193079c9d3628dee99931d31` |
| `lake-manifest.json` | `408ea491dc48a404e8703c56403d3d252fa7bdc4feb393951e34339675a5e2bc` |
| Current and archived `verify_local.py` | `67b74e0aedc0d6896d1ccaf7e57de3e70b156c56e706e77d6c43703a5ef9a128` |
| `original-proof/solution.md` | `6e1bb10bb2ccfe1311775a6d24ca292113036867e080185741ef4f5d5b8174aa` |

The dedicated `.github/workflows/mf21-frozen-verification.yml` is byte-identical to its original reviewed version, SHA256 `eb8a5aecaefab354c61825e5bd3ad1d31b53a7ca880192756581529e8b299012`. I read its committed contents: it selects only the literal project path above, passes the same value to validation and `verify.sh`, disables the Lean action's prebuild/test/cache steps, and invokes the verifier without `sudo` after host preparation. It has no ignored-error override. Its artifact step uses `if: always()`, which preserves failure evidence without converting failure into success.

All **17 shared files** under `tools/lean`, `docs/lean/schema`, and the existing generic Lean workflow remain byte-identical to the reviewed base `aa8d010bdd5a3ef1750c9fa013b67b6d90786d96`; the complete Git diff for those paths is empty. In particular, the existing harness is SHA256 `f81767a17973956fbe9e5765c664d4639cce15ddf8c106f70cdcb32151808c2f`, the source lock is `b3833b07916e5db77579b9cc53ca582282f6a841f36d6a60d693e5b02d342b6b`, and `verify.sh` is `fb97ffb467b0e9acac302b2bdfc418da12b88e4ae307fb0d580c96fd6b63e997`. This confirms continuity with the existing static workflow/harness review; it is not a new runtime isolation test or a new detailed audit of the fetched Comparator implementation.

The frozen project contains the longer `reviews/repaired-package-evidence-review.md`, SHA256 `06b018cebe9fd7911102ffef38a025c0b752e9fd16add6d9eebec26c4e511051`. Its author's condensed final version is retained externally in this directory as `repaired-package-evidence-review.md`, SHA256 `9fc42c4faa92013c4b5ba8c1a8dc1ff5ed4d817fa677a710ca032c453451ecd2`. I read and hashed both: their findings and execution distinctions agree. They are different report artifacts and must retain their respective hashes. No proof or pin difference is attributed to that prose condensation.

The committed `VERIFICATION.md` explicitly identifies itself as the evidence snapshot before the repaired-source Linux run. The completed local record itself says `comparator: not_run` and has no GitHub run ID. The coordinator initially reported dedicated run **35550709155** in progress and subsequently supplied its failed job log. At `2026-09-21T01:34:08Z`, I independently read and hashed `/private/tmp/mf21-verifier-tools/linux-second-failed.log`, SHA256 **`4af5ebea1ad9bb2fdfac68f87685135116e21123cd8b235f0d810c6cdd28f04e`**. The coordinator identifies this log with run 35550709155 at the inspected repaired commit; I did not independently query GitHub.

The log records successful builds of the repaired Numerics at line 485, `MonicRootSums` at line 600, and `PiTranscendental` at line 615. At lines 1303–1307, the actual `PhaseWindowRoots.lean` invocation uses `-j1 -M4096` with Lake's `--setup` and output options, then throws an interpreter `lean::memory_exception` and exits 134. The exception is timestamped `2026-09-21T01:29:51.0075125Z`. Lines 1308–1313 record the failed required target, build failure, Comparator-phase exit 1, and workflow-step exit 2 at `01:29:56.2395121Z`. Earlier acceptance text at lines 198–200 belongs to the `simple_match` regression fixture. It is not MF-21 target acceptance. The log does not isolate the cause of the memory use or establish that a proposed future tactic change resolves it.

Thus the repaired local run remains a genuine source-matched local pass, while **the second actual Linux attempt failed during the project build**. The first attempt 35548010081 also remains failed. No complete-target Comparator or kernel-replay acceptance is established, and no distinct-problem count is added. A later repair and runtime result require their own exact sources, local evidence, checked commit, receipt, logs, and run ID.
